/*global Backbone */

var app = app || {};

(function () {
	'use strict';
	app.Statement = Backbone.Model.extend(
    { defaults: 
      { s: undefined
      , p: undefined
      , o: undefined
		  }
    })

})()
